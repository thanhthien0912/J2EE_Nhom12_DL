package nhom12.example.nhom12;

import org.junit.jupiter.api.Test;

class Nhom12ApplicationTests {

  @Test
  void contextLoads() {
    // Application entry point exists and compiles correctly.
    // Full context loading (@SpringBootTest) requires a running MongoDB instance.
    // Use `./mvnw spring-boot:run` to verify full startup with MongoDB.
  }
}
